package Practice_Prgramms;

public class Lab_31_Switch {
    public static void main(String[] args) {
        int a = 10;

        switch (a)
        {
            //without any statments in switch is possible
        }
        System.out.println("outside the loop");

 //       boolean b = true;
//        switch (b) {
        //boolean is not support in switch
//        }
        long a11 = 30l;
        switch ((int) a11) {

            //long is supported but you need to convert it in to int
        }
        char ch = 'A'; // 65
        switch (ch) {
        }

    }
}
